import { dashboard, bookstore } from './dashboard';
import { utilities, outerLinks } from './utilities';
import { other } from './other';

//-----------------------|| MENU ITEMS ||-----------------------//

const menuItems = {
    items: [dashboard, utilities, other],
    outerItems: [dashboard, outerLinks]

};


export default menuItems;
